CONSTELLATION_API_KEY=
APIFY_API_TOKEN=

# PostgreSQL for test-validator dashboard persistence:
# - Apify scrape result cache (by URL)
# - Saved on-demand job templates (UI reuse)
# - Validation comparison history
DATABASE_URL=postgresql://user:password@localhost:5432/bt13_validator

# Optional: backfill scrape_cache from Apify run history (see scripts/sync_apify_scrape_cache.py)
# SCRAPE_CACHE_TTL_HOURS=168

# Pay-per-event Apify actors require maxTotalChargeUsd >= $0.02 per API run (Console default is $1).
# APIFY_MAX_TOTAL_CHARGE_USD=1.0
